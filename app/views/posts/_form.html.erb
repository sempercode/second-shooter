<%= bootstrap_form_for @post, :html => { :class => "form-vertical"} do |f| %>

<%= f.text_field :title %>

<%= f.text_area :content %>

<%= f.submit 'Submit', :class => 'btn btn-primary' %>

<% end %>

